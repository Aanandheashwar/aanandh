def print_numbers(n):
    for i in range(1, n + 1):
        if (i & (i - 1)) == 0 and (i & n) == i:
            print(i)

if __name__ == '__main__':
    num = 16
    print(f"Numbers between 1 and {num} having first and last bits as the only set bits:")
    print_numbers(num)
